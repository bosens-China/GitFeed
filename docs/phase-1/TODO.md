# GitFeed 第一阶段 TODO

本清单对应 [第一阶段 PRD](./PRD.md)。完成项应同时满足功能、异常处理和验收标准，不能只以界面存在作为完成依据。

已知限制见 [KNOWN_LIMITATIONS.md](./KNOWN_LIMITATIONS.md)。

## 1. 数据模型与基础设施

- [x] 定义仓库工作台模型、筛选状态模型、commit 模型、文件变更模型和统计结果模型。
- [x] 明确仓库唯一标识规则，使用规范化绝对路径防止重复添加。
- [x] 设计每仓库独立的本地持久化结构和版本迁移策略。
- [x] 实现工作台顺序、上次打开仓库和每仓库筛选状态的保存与恢复。
- [x] 将 Git 读取放在不会阻塞 Electron 主进程和渲染进程的执行路径中。
- [x] 建立主进程与渲染进程之间的最小 IPC 接口，并限制可调用的只读 Git 能力。
- [x] 为加载、成功、空结果和失败定义统一状态。

## 2. 仓库工作台

- [x] 实现系统目录选择器。
- [x] 校验目录存在、可读取且属于有效 Git 仓库。
- [x] 阻止重复添加同一规范化仓库路径。
- [x] 实现左侧仓库列表、当前仓库高亮和同名仓库路径区分。
- [x] 实现仓库切换，并恢复仓库独立的筛选状态。
- [x] 实现从工作台移除仓库，确保不删除或修改磁盘文件。
- [x] 实现启动时恢复上次打开的仓库。
- [x] 处理仓库被移动、删除或失去访问权限的状态。

## 3. 只读 Git 数据层

- [x] 选用跨平台命令行库，通过系统 `git` 执行只读命令；本机无 Git 时给出明确错误。
- [x] 获取仓库当前检出的分支和所有本地分支。
- [x] 在不 checkout 的情况下读取任一本地分支在当前时间范围内的提交。
- [x] 读取 commit 的完整 hash、author、author date 和完整 message。
- [x] 读取每个 commit 的文件路径和新增、修改、删除、重命名状态。
- [x] 读取文本文件的新增/删除行数，并正确处理二进制文件。
- [x] 识别 parent 数量大于 1 的 merge commit。
- [x] 按完整 hash 对单次查询结果去重。
- [x] 保证文件名、作者信息和 message 的 Unicode 内容不丢失。
- [x] 验证所有 Git 调用均为只读，且参数不会造成 shell 注入。

## 4. 分支与作者筛选

- [x] 实现仅包含本地分支的分支选择器。
- [x] 首次添加仓库时默认选择当前检出的分支。
- [x] 处理已保存分支被删除的回退逻辑。
- [x] 处理 detached HEAD：按 `main` → `master` → 本地分支列表第一条 回退并提示。
- [x] 从当前分支 + 当前时间范围内的提交生成作者列表（姓名+邮箱）。
- [x] 实现作者多选、全选和按姓名/邮箱搜索。
- [x] 默认选择全部作者。
- [x] 时间范围变化后刷新作者候选项；已选作者失效时清空作者选择。
- [x] 持久化每个仓库的分支和作者状态。
- [x] 验证切换分析分支不会改变仓库 HEAD 或工作区文件。

## 5. 时间与 merge 筛选

- [x] 使用本机时区统一计算和展示时间。
- [x] 以周一 00:00 为边界实现“本周”（左闭右开，结束为当前时间）。
- [x] 实现“上周”的左闭右开边界。
- [x] 以自然月 1 日 00:00 为边界实现“本月”和“上月”。
- [x] 实现自定义开始和结束日期：开始日 `00:00:00`，结束日 `23:59:59`。
- [x] 校验开始时间早于结束时间。
- [x] 默认时间范围设为“本周”。
- [x] 实现“包含 merge commit”开关并默认关闭。
- [x] 持久化每个仓库的时间范围和 merge 开关。

## 6. 统计与提交列表

- [x] 计算当前范围内去重后的提交次数。
- [x] 汇总文本文件新增行数和删除行数。
- [x] 按文件路径去重计算修改文件数。
- [x] 将单次 Git 重命名按一个文件计入修改文件数。
- [x] 实现作者、时间、hash、分析分支和完整 message 展示。
- [x] 实现带变更状态的文件列表。
- [x] 默认按提交时间倒序排列。
- [x] 为大量提交实现虚拟列表，同时保持全量统计准确。
- [x] 确保所有筛选条件同时驱动统计和列表，避免两者范围不一致。

## 7. 刷新与交互状态

- [x] 添加仓库后自动读取数据。
- [x] 启动并恢复仓库时自动刷新。
- [x] 切换仓库时自动刷新。
- [x] 提供手动刷新按钮。
- [x] 刷新后保留当前筛选状态。
- [x] 扫描时展示加载状态，避免重复触发并发扫描。
- [x] 为失败、无 commit 和无筛选结果提供独立空状态。

## 8. Markdown 复制

- [x] 按 PRD 定义实现统一 Markdown 模板。
- [x] 复制仓库、分支、时间、时区、作者和 merge 范围信息。
- [x] 复制提交次数、新增/删除行数和修改文件数。
- [x] 复制当前范围内全部提交的完整明细和文件列表。
- [x] 正确处理 message 中的换行和 Markdown 特殊字符。
- [x] 写入系统剪贴板并反馈成功或失败。
- [x] 无匹配提交时禁用复制按钮。

## 9. 测试与验收

- [x] 为周/月边界、跨年、闰年场景编写时间范围测试（本机时区 `Date` 边界；夏令时依赖系统时区换算）。
- [x] 为自定义日期 `00:00:00`～`23:59:59` 编写筛选测试。
- [x] 为作者多选、全部作者、时间切换后作者置空和空结果编写筛选测试。
- [x] 为失效分支/`detached HEAD` 回退规则编写测试。
- [x] 为文本、二进制、重命名和重复路径编写统计测试。
- [x] 为完整 message、多行正文和特殊字符编写复制测试。
- [x] 验证分析其他分支仅使用只读 Git 命令（`log` / `show` / `for-each-ref` / `rev-parse` / `symbolic-ref`），不执行 checkout。
- [ ] 使用无 commit 仓库、同名仓库和不可访问路径完成手工异常验收。
- [ ] 使用较大提交历史验证虚拟列表响应性和取消/重试能力。
- [ ] 验证系统未安装 Git 时的错误提示。
- [ ] 按 PRD 第 9 节逐条执行产品验收。

## 10. 第一阶段完成定义

- [ ] 所有验收标准通过（剩余手工验收项见上）。
- [x] 没有任何会改变 Git 仓库状态的调用。
- [x] 关键筛选和统计逻辑有自动化测试覆盖。
- [x] macOS 开发构建（`electron-vite build`）已通过；Windows / Linux 安装包尚未手工验收。
- [x] 第一阶段已知限制已记录，第二阶段可以直接复用结构化当前范围数据。
